# Version 1

- run OHIF, Orthanc, Nginx: at root folder run that command
  `docker compose up`
- run Todo API server: to to Todo.Api and run that command
  `dotnet run`
- run the UI: todo-ui/src and run that command
  `ng serve`
